# Read the Docs configuration file for building preview versions of the website.
# See https://docs.readthedocs.io/en/stable/config-file/v2.html for details.
#
version: 2

build:
  os: ubuntu-26.04
  tools:
    python: "3.14"

# Build the HTML pages only. No PDF/ePub versions for a website.
formats: []

sphinx:
  configuration: conf.py

python:
  install:
    - requirements: requirements.txt
